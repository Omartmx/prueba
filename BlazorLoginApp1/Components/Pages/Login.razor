@page "/login"

<div class="login-container">
    <div class="login-card">
        <h2> Iniciar Sesión</h2>
        
        <EditForm Model="@this" OnValidSubmit="HandleLogin" FormName="loginForm">
            <DataAnnotationsValidator />
            <ValidationSummary />
            
            <div class="form-group">
                <label for="email">Email:</label>
                <InputText @bind-Value="email" id="email" class="form-control" />
                <ValidationMessage For="@(() => email)" />
            </div>

            <div class="form-group">
                <label for="password">Contraseña:</label>
                <InputText @bind-Value="password" id="password" type="password" class="form-control" />
                <ValidationMessage For="@(() => password)" />
            </div>

            <button type="submit" class="login-btn">
                Iniciar Sesión
            </button>
        </EditForm>

        @if (showMessage)
        {
            <div class="alert @alertClass">
                @message
            </div>
        }

        <div class="demo-info">
            <h4>Credenciales de prueba:</h4>
            <p><strong>Email:</strong> admin@test.com</p>
            <p><strong>Contraseña:</strong> 123456</p>
        </div>
    </div>
</div>

<style>
    .login-container {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 80vh;
        padding: 20px;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }

    .login-card {
        background: white;
        padding: 2rem;
        border-radius: 10px;
        box-shadow: 0 10px 25px rgba(0,0,0,0.2);
        width: 100%;
        max-width: 400px;
    }

    .login-card h2 {
        text-align: center;
        margin-bottom: 1.5rem;
        color: #333;
    }

    .form-group {
        margin-bottom: 1rem;
    }

    .form-group label {
        display: block;
        margin-bottom: 0.5rem;
        font-weight: bold;
        color: #555;
    }

    .form-control {
        width: 100%;
        padding: 10px;
        border: 2px solid #ddd;
        border-radius: 5px;
        font-size: 16px;
        box-sizing: border-box;
    }

    .form-control:focus {
        outline: none;
        border-color: #667eea;
    }

    .login-btn {
        width: 100%;
        padding: 12px;
        background: #667eea;
        color: white;
        border: none;
        border-radius: 5px;
        font-size: 16px;
        cursor: pointer;
        margin-top: 1rem;
        transition: background 0.3s;
    }

    .login-btn:hover {
        background: #5a6fd8;
    }

    .alert {
        padding: 10px;
        border-radius: 5px;
        margin: 1rem 0;
        text-align: center;
    }

    .alert-success {
        background: #d4edda;
        color: #155724;
        border: 1px solid #c3e6cb;
    }

    .alert-error {
        background: #f8d7da;
        color: #721c24;
        border: 1px solid #f5c6cb;
    }

    .demo-info {
        margin-top: 1.5rem;
        padding: 1rem;
        background: #f8f9fa;
        border-radius: 5px;
        border-left: 4px solid #667eea;
    }

    .demo-info h4 {
        margin: 0 0 0.5rem 0;
        color: #333;
    }

    .demo-info p {
        margin: 0.25rem 0;
        font-size: 0.9rem;
    }
</style>

@code {
    [SupplyParameterFromForm]
    public string email { get; set; } = "";
    
    [SupplyParameterFromForm]
    public string password { get; set; } = "";
    
    private string message = "";
    private bool showMessage = false;
    private string alertClass = "";

    private async Task HandleLogin()
    {
        try 
        {
            Console.WriteLine($"🔴 Login intentado - Email: {email}, Password length: {password?.Length ?? 0}");

            if (string.IsNullOrWhiteSpace(email) || string.IsNullOrWhiteSpace(password))
            {
                message = "❌ Por favor, completa todos los campos";
                alertClass = "alert-error";
                showMessage = true;
                StateHasChanged();
                return;
            }

            Console.WriteLine("Campos validados, verificando credenciales...");

            if (email == "admin@test.com" && password == "123456")
            {
                Console.WriteLine("Credenciales correctas");
                message = "¡Login exitoso! Bienvenido admin@test.com";
                alertClass = "alert-success";
                showMessage = true;
                
                // Limpiar campos
                email = "";
                password = "";
            }
            else
            {
                Console.WriteLine("❌ Credenciales incorrectas");
                message = "❌ Credenciales incorrectas. Usa: admin@test.com / 123456";
                alertClass = "alert-error";
                showMessage = true;
            }

            // Forzar actualización de la UI
            await InvokeAsync(StateHasChanged);
        }
        catch (Exception ex)
        {
            Console.WriteLine($"❌ Error en login: {ex.Message}");
            message = "❌ Error al procesar el login. Por favor, intenta de nuevo.";
            alertClass = "alert-error";
            showMessage = true;
            await InvokeAsync(StateHasChanged);
        }
    }
}